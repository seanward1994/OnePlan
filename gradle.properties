# Gradle properties (can be empty for CI convenience)
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
# Open Actions to download artifacts: Actions → OnePlanAndroid — CI → latest run.
